
  <h1>Paniers près de <%= @query %></h1>
  <div class="d-flex">
    <div class="index-panier-grid">
      <% @paniers_alentour.each  do |panier| %>
      <div class="index-panier-card p-3">
        <p><%= panier.titre %></p>
        <p><%= panier.tag_list %></p>
      </div>
    <% end %>
    </div>
    <div
    id="map"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
    ></div>
  </div>



